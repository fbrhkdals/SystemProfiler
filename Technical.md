# Technical Background & Glossary

본 프로젝트는 Windows 운영체제의 **커널(Kernel) 인터페이스**와 **CPU 레지스터** 수준에서 데이터를 추출하여 분석합니다. UI에 출력되는 각 용어와 기술적 원리는 다음과 같습니다.

---

## 1. CPU & Thread Execution (레지스터 분석)

* **RIP (Instruction Pointer):** CPU가 다음에 실행할 기계어 명령의 메모리 주소를 담고 있는 64비트 레지스터입니다. 현재 스레드가 코드의 정확히 어느 지점을 실행 중인지 나타내는 '지침서'와 같습니다.
* **TID (Thread ID):** 프로세스 내에서 실제 연산을 수행하는 최소 단위인 스레드의 고유 번호입니다.
* **OFFSET:** 모듈(DLL/EXE)의 시작 주소로부터 현재 실행 지점(RIP)까지의 거리입니다. 심볼(PDB)이 없을 때 특정 코드 위치를 식별하는 유일한 단서가 됩니다.
* **Stack Walking:** `RSP(Stack Pointer)`와 `RBP(Base Pointer)`를 추적하여 현재 함수를 호출한 부모 함수들의 경로를 역추적하는 기술입니다.



---

## 2. Memory Management (메모리 지표)

* **Private Bytes (전용 메모리):** 해당 프로세스가 독점적으로 사용 중인 메모리 양입니다. 다른 프로세스와 공유되지 않으므로, 이 수치가 계속 상승하면 **메모리 누수(Memory Leak)**를 의미합니다.
* **Working Set (물리 메모리):** 프로세스가 현재 사용 중인 실제 RAM 용량입니다.
* **Virtual Size (가상 메모리):** 프로세스에게 할당된 전체 가상 주소 공간의 크기입니다. 실제 물리 RAM보다 훨씬 큰 범위를 가집니다.
* **Page Faults (페이지 폴트):** 프로세스가 필요한 데이터가 RAM에 없어 하드디스크의 가상 메모리(Page File)에서 데이터를 가져온 횟수입니다. 빈번한 발생은 시스템 성능 저하의 원인이 됩니다.



---

## 3. Windows Architecture (커널 및 유저 모드)

* **User Mode (유저 모드):** 일반 어플리케이션이 실행되는 제한된 권한의 영역입니다. 본 프로그램의 UI 로직 등이 여기서 실행됩니다.
* **Kernel Mode (커널 모드):** 운영체제 핵심 코드가 실행되는 영역입니다. `Kernel CPU` 점유율은 파일 I/O, 드라이버 제어 등 OS 서비스 호출에 사용된 시간을 의미합니다.
* **System Calls (시스템 콜):** 유저 모드에서 커널 기능을 사용하기 위한 인터페이스입니다. 코드에서 `ntdll.dll` 내부 함수들이 이 역할을 수행합니다.



---

## 4. Resource & Priority

* **Handle Count (핸들 개수):** 프로세스가 열어둔 파일, 소켓, 이벤트 등 커널 객체에 대한 참조 개수입니다.
* **Priority Class (우선순위):** CPU 스케줄러가 어떤 프로세스에 자원을 먼저 할당할지 결정하는 등급입니다. (`Idle` < `Normal` < `High` < `Realtime`)
* **Logical Cores (논리 코어):** 시스템에 존재하는 CPU 코어 수입니다. 하이퍼스레딩이 적용된 경우 실제 물리 코어 수보다 많을 수 있으며, `Max: 800%`와 같은 수치는 8코어가 모두 풀가동 중임을 뜻합니다.

---

## 5. Debugging & Analysis

* **PDB (Program Database):** 기계어 주소를 함수 이름(`Main()`)과 소스 코드 라인으로 변환해주는 지도 파일입니다.
* **Symbol Resolution:** 메모리 주소(`0x7FF...`)를 사람이 읽을 수 있는 텍스트로 바꾸는 과정입니다. PDB가 없으면 주소값만 출력됩니다.
* **Thread Sampling:** 스레드를 짧은 간격으로 정지(`Suspend`)시키고 상태를 기록한 뒤 재개(`Resume`)하는 분석 기법입니다. 통계적으로 가장 많이 포착된 지점이 성능 병목(**Hot Spot**)입니다.



---

이 가이드는 시스템 프로그래밍의 깊은 이해를 돕기 위해 작성되었습니다. 코드가 하드웨어 레벨에서 어떻게 상호작용하는지 이해하는 데 도움이 되길 바랍니다.
